\chapter{基于 Render Graph 的渲染器架构设计}

\section{混合渲染管线总体架构}
本系统的混合渲染管线被解耦为多个功能单一的渲染通道（Render Pass）。在 Render Graph 架构下，整个渲染流程不再是线性的函数调用，而是被抽象为一张由“计算节点”与“数据流”构成的图结构。

本系统的核心管线总体架构由以下四个关键阶段节点（Pass Nodes）构成：

\begin{enumerate}
    \item \textbf{几何缓冲通道（G-Buffer Pass）}：作为整个图结构的输入端，采用传统光栅化管线。负责提取场景中可见几何体的物理属性，输出包含反照率（Albedo）、世界空间法线（Normal）、深度（Depth）、材质参数（Metallic/Roughness）以及运动矢量（Motion Vector）的多张屏幕空间纹理。
    
    \item \textbf{光线追踪通道（Ray Tracing Pass）}：声明对 G-Buffer 纹理的读取依赖。利用 Vulkan 硬件加速指令，基于几何法线与深度重建世界空间坐标，并发射射线计算硬/软阴影与镜面反射，输出原始的 1 SPP（Sample Per Pixel）光照噪点图。
    
    \item \textbf{时空降噪通道（SVGF Denoising Pass）}：包含多个计算着色器（Compute Shader）子节点。接收上一帧的历史累积缓冲与当前帧的光追输出，利用运动矢量进行时域重投影，并进行多轮 A-Trous 空域滤波，输出平滑的光照信号。
    
    \item \textbf{合成与后处理通道（Composition \& Post-Process Pass）}：处于图结构的末端。将降噪后的光照信号与 G-Buffer 中的基础颜色进行物理调制（Modulate），最后执行色调映射（Tone Mapping）输出至交换链（Swapchain）呈现给终端用户。
\end{enumerate}

% [此处插入图3-1：基于 Render Graph 的混合渲染管线总体拓扑图]
% \begin{figure}[htb]
%   \centering
%   \includegraphics[width=0.8\textwidth]{figures/chapter3/render_graph_topology.png}
%   \caption{基于 Render Graph 的混合渲染管线总体拓扑图}
%   \label{fig:render_graph_topology}
% \end{figure}

\section{Render Graph 核心机制与数据结构}
为了在 C++ 中用面向对象的方式描述上述管线，本系统设计了一套轻量级的虚拟资源与节点管理机制。Render Graph 的核心数据结构由“虚拟资源（Virtual Resource）”与“渲染通道（Pass Node）”两部分组成。

\subsection{虚拟资源池化管理}
在图的构建阶段（Setup Phase），系统并不立即调用 Vulkan API 分配物理显存（VkImage 或 VkBuffer），而是分配轻量级的虚拟资源句柄（Virtual Resource Handle）。

系统定义了 TextureResource 与 BufferResource 数据结构，记录资源的分辨率、格式（Format）与生命周期。只有在 Render Graph 编译阶段（Compile Phase），系统才会通过计算各资源的生命周期区间，利用 Vulkan 内存分配器（VMA, Vulkan Memory Allocator）为生命周期不重叠的资源进行物理内存的别名复用（Memory Aliasing），从而极大降低了系统整体的显存占用峰值。

\subsection{渲染通道构建器（Pass Builder）}
每个具体的渲染阶段通过继承基类 RenderPass 实现。每个 Pass 在执行具体的 GPU 录制之前，必须通过 PassBuilder 显式声明其对虚拟资源的依赖关系：
\begin{itemize}
    \item \textbf{Read(ResourceHandle)}：声明该通道需要读取该资源，向图中添加一条从资源到节点的有向边。
    \item \textbf{Write(ResourceHandle)}：声明该通道将覆写或创建该资源，向图中添加一条从节点到资源的有向边。
\end{itemize}
通过这种声明式 API，Render Graph 能够在 CPU 端静态捕获所有节点间的数据依赖，进而构建出完整的有向无环图。

\subsection{跨帧资源与乒乓轮换机制（Ping-Pong Mechanism）}
在时域抗锯齿（TAA）与时空降噪（SVGF）算法中，当前帧的计算强依赖于上一帧的输出结果（历史缓冲）。这种跨越单帧生命周期的资源流转打破了传统 Render Graph 仅限单帧分析的局限。
为了解决 Compute Shader 同时对同一物理资源进行读写的“资源别名（Resource Aliasing）”数据冒险（Data Hazard），本系统在架构中引入了乒乓轮换机制。系统为历史数据分配两张物理隔离的长期存活贴图（Persistent Images）。在图构建时，利用当前帧索引的奇偶性（\texttt{FrameIndex \% 2}）动态切换绑定的资源视图：在偶数帧读取贴图 A 并写入贴图 B，奇数帧则反之。结合渲染图底层的 \texttt{vkCmdCopyImage} 物理硬拷贝逻辑，彻底杜绝了时域算法中常见的画面横跳与闪烁伪影。

\section{资源状态与显存屏障（Barrier）的自动推导}
Vulkan 规范要求，当同一个资源在不同管线阶段（Pipeline Stages）被连续访问，且至少有一次是写操作时，必须插入显式的管线屏障（VkImageMemoryBarrier2 或 VkBufferMemoryBarrier2）。本系统 Render Graph 的最大技术贡献，在于实现了屏障的自动推导引擎。

\subsection{拓扑排序与执行剔除}
在图编译阶段，系统首先利用深度优先搜索（DFS）算法对构建好的有向无环图进行拓扑排序（Topological Sort），确定所有渲染通道的线性执行序列。同时，如果某个 Pass 产生的输出资源最终没有被终端节点（如交换链输出）所依赖，系统会将其视为冗余计算（Dead Code）并将其从执行队列中剔除，从而实现极致的性能优化。

\subsection{屏障参数的动态化生成}
在执行排序后的线性队列时，Render Graph 会通过维护一个全局的资源状态跟踪器（Resource State Tracker），记录每个物理资源当前的访问掩码（Access Mask）与图像布局（Image Layout）。

当相邻的通道 Pass\_A 与 Pass\_B 对同一个图像资源 R 产生依赖时，系统通过查表机制自动推导屏障参数：
\begin{enumerate}
    \item \textbf{读写同步（Execution Dependency）}：系统自动推导 \texttt{srcStageMask} 与 \texttt{dstStageMask}，例如强制 GPU 在执行 Pass\_B 的 Compute Shader 读取前，必须等待 Pass\_A 的 Color Attachment 写入操作彻底刷新至显存缓存（Flush to VRAM），从根本上避免了经典的数据撕裂与读取过期（Stale Data）问题。
    \item \textbf{常规状态转换（Layout Transition）}：假设 Pass\_A 是 G-Buffer 生成阶段，其将 R 作为颜色附件写入，此时 R 处于 \texttt{COLOR\_ATTACHMENT\_OPTIMAL} 状态；Pass\_B 为光线追踪阶段，需将 R 作为纹理进行采样。系统会自动生成屏障，将其安全地转换为 \texttt{SHADER\_READ\_ONLY\_OPTIMAL}。针对 Compute Shader 的读写需求，则自动转换为通用的 \texttt{GENERAL} 布局。
    \item \textbf{深度缓冲状态推导（Depth-only Constraints）}：Vulkan 规范严格禁止纯深度格式（如 \texttt{D32\_SFLOAT}）转换为 \texttt{GENERAL} 布局。针对此时空降噪网络中频繁使用的历史深度缓冲（History Depth），本系统架构引入了特殊的 \texttt{isDepth} 标志位路由。在屏障自动生成时，若检测到该资源为深度图，则绕过默认的 \texttt{GENERAL} 转换策略，将其强制约束在 \texttt{DEPTH\_STENCIL\_READ\_ONLY\_OPTIMAL} 或 \texttt{SHADER\_READ\_ONLY\_OPTIMAL} 状态，从而满足了 Vulkan Validation Layer 极其严苛的状态校验要求。
\end{enumerate}

% [此处插入图3-2：资源状态自动推导与屏障插入机制原理图]
% \begin{figure}[htb]
%   \centering
%   \includegraphics[width=0.8\textwidth]{figures/chapter3/barrier_derivation.png}
%   \caption{资源状态自动推导与屏障插入机制原理图}
%   \label{fig:barrier_derivation}
% \end{figure}

\section{本章小结}
本章详细阐述了基于 Render Graph 的渲染引擎架构设计。针对 Vulkan 显式状态管理的复杂性，本系统摒弃了传统的硬编码调度模式，设计了包含虚拟资源句柄与有向无环图分析的自动化管线系统。通过拓扑排序、资源状态跟踪机制以及针对时域算法定制的跨帧乒乓轮换结构，系统成功实现了管线依赖关系的自动解耦、冗余阶段的动态剔除以及复杂显存屏障（尤其是深度缓冲的非法布局约束）的安全推导。该架构不仅彻底解决了混合渲染管线中多阶段穿插执行的同步难题，更为后续各种复杂光照算法与降噪网络的高效集成提供了一个强健、现代化的底层底座。