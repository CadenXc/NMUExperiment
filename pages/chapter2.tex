% !Mode:: "TeX:UTF-8"

\chapter{实时混合渲染理论基础}
\label{chap:theory}

本章将阐述构建实时混合渲染引擎所需的理论基石。首先介绍基于物理的渲染（PBR）数学模型，随后探讨现代 Vulkan API 的显式驱动机制，最后详细解析硬件加速光线追踪的底层原理，为后续章节中渲染图架构与降噪算法的实现奠定理论基础。

\section{基于物理的渲染（PBR）理论}

\subsection{微面元理论与 Cook-Torrance BRDF}
% 核心点：解释材质表面如何与光交互
为了模拟真实世界的材质表现，通常采用微面元模型（Microfacet Theory），假设表面由无数微小的镜面组成。微面曲面模型通常由给出微面法线 $n_f$ 关于曲面法线 $n$ 的分布函数来描述，如图 \ref{fig:microfacet_model} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/chapter2/Microplane_reflection.png}
    \caption{微面元模型示意图：(a) 微面法线变化越大，表面越粗糙；(b) 光滑表面的微面法线变化相对较小}
    \label{fig:microfacet_model}
\end{figure}

其核心的双向反射分布函数（BRDF）采用 Cook-Torrance 模型：
\begin{equation}
    f_r(p, \omega_i, \omega_o) = \frac{D(h)F(v, h)G(l, v, h)}{4(n \cdot l)(n \cdot v)}
\end{equation}
其中，$D(h)$ 为法线分布函数（NDF），$F$ 为菲涅尔项（Fresnel），$G$ 为几何遮蔽项（Geometry）。本文重点讨论 Trowbridge-Reitz (GGX) 分布在实时渲染中的应用。

\subsection{渲染方程与蒙特卡洛积分}
% 核心点：全局光照的数学本质
场景中任意点的出射辐亮度 $L_o$ 由 Kajiya 提出的渲染方程描述。在实时端，由于解析解难以求得，通常采用蒙特卡洛积分（Monte Carlo Integration）进行数值估算：
\begin{equation}
    E[f(x)] \approx \frac{1}{N} \sum_{i=1}^{N} \frac{f(x_i)}{p(x_i)}
\end{equation}
在 1 SPP（每像素一采样）下，如何通过重要性采样（Importance Sampling）降低方差是使用 Temporal 技术的好场景。

\section{现代显式图形 API（Vulkan）架构}

\subsection{显式资源管理与同步机制}
% 核心点：Vulkan 相比 OpenGL 的核心优势
不同于传统的图形学 API，Vulkan 是现代的图形学API，提供给用户更细致的权限，让开发者更细致的控制。
\begin{itemize}
    \item \textbf{内存管理}：论述显存堆（Memory Heap）的手动分配与缓冲区（Buffer）/图像（Image）的绑定流程。
    \item \textbf{同步与屏障}：分析 Pipeline Barrier 与 Render Pass 依赖如何解决 GPU 上的数据竞争（RAW/WAR/WAW）。
\end{itemize}

\subsection{Vulkan 动态渲染}
% 核心点：本项目用到的现代化特性
本文利用了 Vulkan 1.3 引入的动态渲染（Dynamic Rendering）特性， 传统Vulkan渲染需要预先创建渲染通道(RenderPass)和帧缓冲(Framebuffer)对象，而动态渲染允许开发者直接在命令缓冲区中开始和结束渲染操作， 省去了传统的 Render Pass 对象创建，简化了渲染图（Render Graph）的节点构建流程，提高了管线切换的灵活性。

\section{硬件加速光线追踪原理}

\subsection{层次包围盒（BVH）加速结构}
% 核心点：AS 的层级结构
为了实现高效的射线-几何体求交，现代 GPU 引入了双层加速结构（Acceleration Structures），其层级拓扑关系如图 \ref{fig:bvh_structure} 所示。

\begin{figure}[H]
    \centering
    \includegraphics[width=0.8\textwidth]{figures/chapter2/bvh_structure_tlas_blas.png}
    \caption{硬件加速的光线追踪双层包围盒（BVH）架构示意图}
    \label{fig:bvh_structure}
\end{figure}

\begin{itemize}
    \item \textbf{底层加速结构（BLAS）}：存储原始顶点数据与索引，负责局部的交点计算。驱动程序会根据几何体的拓扑结构自动构建最优的二叉树。
    \item \textbf{顶层加速结构（TLAS）}：存储多个 BLAS 的实例引用（Instance）及其对应的 4x4 世界变换矩阵。这种解耦设计使得场景中物体的平移、旋转仅需重构轻量级的 TLAS，而无需改动庞大的顶点数据，从而支持了动态场景的实时渲染。
\end{itemize}

\subsection{光线追踪管线（RTP）与光线查询（Ray Query）}
% 核心点：两种执行方式的区别
Vulkan 
\begin{itemize}
    \item \textbf{RT Pipeline}：通过 RayGen、Miss、ClosestHit 等专用着色器阶段实现复杂的路径追踪逻辑。
    \item \textbf{Ray Query}：允许在任意着色器阶段（如 Fragment Shader）发起射线探测，适用于混合渲染中的硬阴影与环境光遮蔽计算。
\end{itemize}

\section{混合渲染（Hybrid Rendering）逻辑流程}

\subsection{延迟渲染与 G-Buffer 提取}
% 核心点：管线的第一步
混合渲染管线的第一阶段采用光栅化方式生成几何缓冲区（G-Buffer），提取场景的深度（Depth）、法线（Normal）、反照率（Albedo）及材质参数。这种方式减少了后续光线追踪管线的计算量，为后续光线追踪管线的计算提供了必要的信息。

\subsection{基于空间与时域的信号重建}
% 核心点：引出后续章节的 SVGF
由于 1 SPP 的光追结果存在严重的统计噪声，必须结合 G-Buffer 提供的辅助信息进行信号重建。本文将利用 G-Buffer 中的运动矢量（Motion Vector）进行时域重投影，并通过空间域滤波器实现最终的平滑输出。
